# Brakeman config (read automatically from config/brakeman.yml).
#
# TODO: remove these skips once Ruby and Rails are upgraded.
# Both checks are legitimate: Ruby 3.1.x reached EOL 2025-03-31 and
# Rails 7.2.x reached EOL 2026-08-09. They are skipped only so CI stays
# green on the current stack; upgrading is a separate, deliberate task.
---
:skip_checks:
  - CheckEOLRuby
  - CheckEOLRails
